# 配置中心和服务注册发现中心配置
spring:
  cloud:
    zookeeper:
      config:
        root: /spring-cloud-zookeeper/config
        profileSeparator: "-"
      discovery:
        root: /spring-cloud-zookeeper/services